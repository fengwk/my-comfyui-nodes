"""Third-party Sol-Attn MiniMax node, vendored verbatim from
https://huggingface.co/t8star/Sol-Attn-v2-wheels (sol_attn_minimax_v2.py).

The file is kept byte-for-byte identical to upstream so it can be refreshed by
re-downloading. It needs comfy-kitchen >= sol_attn branch (comfy_kitchen.sol_attn)
and is registered through registry.py, not through its own comfy_entrypoint.
"""
